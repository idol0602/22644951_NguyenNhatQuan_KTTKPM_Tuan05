version: '3.8'

services:
  flask:
    build: .
    container_name: flask_app
    environment:
      FLASK_APP: app.py
      FLASK_ENV: development
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    networks:
      - flask_network

networks:
  flask_network:
    driver: bridge
